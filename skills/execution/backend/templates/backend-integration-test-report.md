# 后端接口测试报告

> 由 `$backend integration-test run [scope]` 或 `$backend integration-test generate [scope]` 生成。保留全部一级标题；没有内容的板块写“无”。

## 1. 基本信息

| 项目 | 内容 |
|---|---|
| 需求 / 任务 | `<名称或链接>` |
| 服务 / 模块 | `<service / module>` |
| 语言 / 框架 | `<language / framework>` |
| 测试运行器 | `<runner and version if known>` |
| 测试层级 | `<接口入口测试 / 组件集成测试 / 端到端测试 / 混合>` |
| 测试版本 | 分支：`<branch>`；Commit：`<commit>` |
| 工作区状态 | `<clean / 含未提交变更；摘要>` |
| 测试环境 | `<local / test / daily / staging>` |
| 环境隔离等级 | `<本地隔离 / 共享测试 / 生产或未知（应阻塞）>` |
| 测试执行时间 | `<start> - <end>` |
| 报告生成时间 | `<datetime>` |

## 2. 测试结论

- 总体结论：`通过 / 有条件通过 / 不通过`
- 结论说明：`<基于实际结果填写>`

| 指标 | 结果 |
|---|---:|
| 接口测试总数 | `<n>` 条 |
| 通过数 | `<n>` 条 |
| 失败数 | `<n>` 条 |
| 阻塞数 | `<n>` 条 |
| 跳过数 | `<n>` 条 |
| 用例通过率 | `<n>%` |

统计口径：总数 = 通过数 + 失败数 + 阻塞数 + 跳过数。失败用例必须在执行明细中标记“待转遗留问题”。

## 3. 测试范围与边界

### 3.1 范围来源

| 项目 | 内容 |
|---|---|
| 范围来源 | `用户指定 / 代码变更自动识别后人工确认 / generate 人工确认后生成` |
| Git 比较基准 | `<base branch / commit / merge-base>` |
| 纳入的工作区变更 | `<files or 无>` |
| 新增或修改的测试文件 | `<test files or 无>` |
| 用户确认记录 | `<确认后的接口、包、测试文件或测试名称>` |
| 共享环境副作用确认 | `<不适用 / 用户确认原文或可复核记录>` |
| 确认后范围变化 | `<无 / 重新确认说明>` |

### 3.2 环境与数据影响

| 项目 | 内容 |
|---|---|
| 目标资源与数据作用域 | `<临时资源 / tenant / account / record IDs / 无>` |
| 预期及实际副作用 | `<数据库、缓存、消息、对象存储、外部调用 / 无>` |
| 清理方式与结果 | `<自动回滚 / 定点清理及结果 / 无>` |
| 失败恢复方式与结果 | `<补偿、回滚、人工恢复 / 无>` |
| 隔离证据 | `<临时容器、事务回滚、唯一命名或共享环境说明>` |

### 3.3 接口与测试范围

| 接口 / 入口 | 关联代码变更 | 测试模块 / 文件 | 测试名称 | 测试层级 | 纳入理由 |
|---|---|---|---|---|---|
| `<METHOD /path>` | `<file:line>` | `<module / file>` | `<test>` | `<level>` | `<evidence>` |

### 3.4 结果验证边界

本次测试实际断言并验证：

- `<HTTP 状态码、业务响应码或关键返回字段>`
- `<数据库状态、缓存、消息或事务结果；未断言则不要填写>`

不适用或未被现有测试断言验证的项目：`<items or 无>`

fake / mock / stub 边界：`<被替身的业务组件、数据库、消息或外部系统；无则写无>`

> 未被现有测试断言覆盖的结果，不得根据 HTTP 200 或测试进程退出码推定为已验证。

## 4. 测试执行情况

### 4.1 执行命令

| 序号 | 工作目录 | 实际命令 | 开始时间 | 耗时 | 退出状态 |
|---|---|---|---|---|---|
| 1 | `<cwd>` | `<command>` | `<time>` | `<duration>` | `<exit code / blocked>` |

### 4.2 用例执行明细

| 用例编号 | 接口 / 模块 | 测试名称 | 测试层级 | 测试场景 | 预期结果 | 实际结果 | 状态 | 遗留问题 |
|---|---|---|---|---|---|---|---|---|
| IT-001 | `<interface / module>` | `<test>` | `<level>` | `<scenario>` | `<expected>` | `<actual>` | `通过 / 失败 / 阻塞 / 跳过` | `无 / 待转遗留问题 / issue link>` |

## 5. 自动执行证据

| 证据编号 | 关联命令 / 用例 | 脱敏日志摘要 |
|---|---|---|
| EV-001 | `<command / case>` | `<actual output excerpt>` |

本报告不采集或管理截图、Postman/Apipost 调试结果及其他人工附件。

## 6. 最终确认

- [ ] 测试数量统计与执行明细一致
- [ ] 语言、框架、测试运行器和测试层级有仓库证据
- [ ] 实际执行范围未超出用户指定或确认范围
- [ ] 使用自动识别范围时，候选测试已获得用户明确确认
- [ ] 使用 generate 时，测试文件和用例计划已获得用户明确确认
- [ ] generate 未修改生产代码或确认范围外的测试文件
- [ ] 失败用例已标记为“待转遗留问题”或填写真实问题链接
- [ ] 测试范围与实际验证边界已经说明
- [ ] fake / mock / stub 隔离的真实集成边界已经说明
- [ ] 环境隔离等级、数据作用域、副作用、清理和失败恢复结果已经说明
- [ ] 共享测试环境的副作用已获得独立明确确认；本地隔离环境写明隔离证据
- [ ] 报告中的敏感信息已经脱敏
- [ ] 报告结论可以由执行命令和日志摘要复核
