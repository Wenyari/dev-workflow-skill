# Figma Sync Snapshots

此目录用于存放 figmaSync 相关的本地报告。

## 文件说明

- `last-sync.md`：最近一次 figmaSync 同步摘要。

## 注意

- 当前 token 变更以 `theme/dark/*.css` diff 为准。
- 不要手动修改报告；需要恢复旧 token 迁移能力时再单独设计迁移方案。
- 修改后必须走 PR review，禁止直接 push 到 main。
