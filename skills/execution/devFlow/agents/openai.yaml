interface:
  display_name: '研发工作流（兼容）'
  short_description: '将旧 devFlow 命令映射到拆分后的前后端工作流'
  default_prompt: '解析用户显式提供的旧 $devFlow 命令，并按兼容表路由到新的前端、后端、PRD Review 或飞书共享规则。'

policy:
  allow_implicit_invocation: false
