# Artifact 能力规划

Artifact 层用于承载跨环节、需要被多个 skill 持续读写的数据产物。当前尚未投产，不进入安装包或用户命令入口。

候选方向：

- **未闭环清单**（open-issues）：收口评审中未当场闭环的问题。
- **校准清单**（calibration）：记录自测遗漏、归因和对评审规则的反馈。

启动任一能力前，必须先通过 ADR 明确字段 schema、写入者、读取者、生命周期和冲突处理规则；实现完成并经过真实流程验证后，再加入 `skills/`、安装器和用户导航。
