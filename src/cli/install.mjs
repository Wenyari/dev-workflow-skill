// Step 1 骨架：只打印欢迎语后退出，用来验证 npx 入口链路。
export async function run() {
  console.log('')
  console.log('📦 @dev-workflow/skill installer')
  console.log('')
  console.log('（骨架就位。Step 2 会接上交互与文件复制。）')
  console.log('')
}
